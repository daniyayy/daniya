#include <iostream>
#include <string>

int main()

{
    int power=10, luck=8, smart=5;
    std::cout << " Rimag: Hello wandering! How can i help you? " << std::endl;
    std::cout << " -Hi, i came to be a part of Guild of fighters. " << std::endl;
    std::cout << " Rimag: Let's see how strong you are. " << std::endl;
    
    if (power >= 9)
    
    std::cout << " You are very strong, so you can join us! " << std::endl;
    
    else
    std::setlocale ;
    
    std::cout << " You are not strong enough, so go back home! " << std::endl;
    
    return 0;
}
    
